class Contacto:
    def __init__(self, nombre, numero, email):
        self.nombre = nombre
        self.numero = numero
        self.email = email

    def mostrar_info(self):
        print("Nombre:", self.nombre)
        print("Numero:", self.numero)
        print("Email:", self.email)

class ContactoTrabajo(Contacto):
    def __init__(self, nombre, numero, email, empresa):
        super().__init__(nombre, numero, email)
        self.empresa = empresa

class ContactoFamilia(Contacto):
    def __init__(self, nombre, numero, email, parentesco):
        super().__init__(nombre, numero, email)
        self.parentesco = parentesco

    def mostrar_info(self):
        super().mostrar_info()
        print("Parentesco:", self.parentesco)

class Agenda:
    def __init__(self):
        self.contactos = []

    def agregar_contacto(self, contacto):
        self.contactos.append(contacto)

    def mostrar_todos(self):
        for contacto in self.contactos:
            contacto.mostrar_info()
            print("---")
            
trabajo1 = ContactoTrabajo("Pedro", "8888-8888", "pedro@empresa.com", "Empresa XYZ")
familia1 = ContactoFamilia("María", "8444-6336", "maria@gmail.com", "Hermana")

mi_agenda = Agenda()
mi_agenda.agregar_contacto(trabajo1)
mi_agenda.agregar_contacto(familia1)
mi_agenda.mostrar_todos()
